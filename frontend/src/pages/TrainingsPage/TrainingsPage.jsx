import Layout from '../../components/layout/Layout/Layout';

// Stub page — route is live at /trainings but has no content, styles, or CSS module.
// Consider adding a "coming soon" placeholder (like ProfilePage) or removing the route until ready.
const TrainingsPage = () => {
    return (
        <Layout>
            <h1>TrainingsPage</h1>
        </Layout>
    );
};

export default TrainingsPage;
